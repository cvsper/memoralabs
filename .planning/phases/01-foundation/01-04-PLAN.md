---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-03"]
files_modified:
  - app/main.py
  - app/routers/health.py
  - render.yaml
  - tests/test_health.py
autonomous: true

must_haves:
  truths:
    - "GET /health returns 200 with status, timestamp, version fields"
    - "FastAPI lifespan initializes system DB and TenantDBManager on startup"
    - "FastAPI lifespan closes all connections on shutdown"
    - "render.yaml configures persistent disk at /data and correct start command"
    - "Keep-alive is documented for UptimeRobot setup (external, free)"
  artifacts:
    - path: "app/main.py"
      provides: "FastAPI app with lifespan, mounts health router"
      exports: ["app"]
    - path: "app/routers/health.py"
      provides: "Health endpoint"
      exports: ["router"]
    - path: "render.yaml"
      provides: "Render deployment config"
      contains: "mountPath: /data"
    - path: "tests/test_health.py"
      provides: "Health endpoint tests"
      min_lines: 20
  key_links:
    - from: "app/main.py"
      to: "app/db/system.py"
      via: "init_system_db in lifespan"
      pattern: "init_system_db"
    - from: "app/main.py"
      to: "app/db/manager.py"
      via: "TenantDBManager in lifespan"
      pattern: "TenantDBManager"
    - from: "app/main.py"
      to: "app/routers/health.py"
      via: "app.include_router"
      pattern: "include_router.*health"
---

<objective>
Wire the FastAPI application together: lifespan managing system DB and TenantDBManager, health endpoint, Render deployment config, and keep-alive cron documentation.

Purpose: This is the entry point that ties all infrastructure together. After this plan, `uvicorn app.main:app` starts a running service with a health endpoint and all DB infrastructure initialized.
Output: Running FastAPI app with /health endpoint, render.yaml for deployment, and keep-alive instructions.
</objective>

<execution_context>
@/Users/sevs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sevs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FastAPI app with lifespan + health router + render.yaml</name>
  <files>
    app/main.py
    app/routers/health.py
    render.yaml
  </files>
  <action>
    Create `app/routers/health.py`:
    - Create `router = APIRouter()`
    - `@router.get("/health")` endpoint returning:
      ```json
      {"status": "healthy", "timestamp": "<UTC ISO string>", "version": "0.1.0"}
      ```
    - Use `datetime.now(timezone.utc).isoformat()` for timestamp
    - No auth required on this endpoint (will be explicitly exempted in Phase 3)

    Create `app/main.py`:
    - Import `FastAPI`, `asynccontextmanager` from contextlib
    - Import `init_system_db` from `app.db.system`
    - Import `TenantDBManager` from `app.db.manager`
    - Import `DATA_DIR, MAX_TENANT_CONNECTIONS` from `app.config`
    - Import `router as health_router` from `app.routers.health`

    - `@asynccontextmanager async def lifespan(app: FastAPI)`:
      - Startup: `DATA_DIR.mkdir(parents=True, exist_ok=True)`
      - `app.state.system_db = await init_system_db(DATA_DIR)`
      - `app.state.tenant_manager = TenantDBManager(data_dir=DATA_DIR, max_connections=MAX_TENANT_CONNECTIONS)`
      - `yield`
      - Shutdown: `await app.state.tenant_manager.close_all()`
      - `await app.state.system_db.close()`

    - `app = FastAPI(title="MemoraLabs", description="Memory-as-a-Service API", version="0.1.0", lifespan=lifespan)`
    - `app.include_router(health_router)`

    Create `render.yaml` (exact content from RESEARCH.md):
    ```yaml
    services:
      - type: web
        name: memoralabs-api
        runtime: python
        buildCommand: pip install -r requirements.txt
        startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
        plan: starter
        disk:
          name: memoralabs-data
          mountPath: /data
          sizeGB: 1
        envVars:
          - key: DATA_DIR
            value: /data
          - key: PYTHON_VERSION
            value: "3.11"
    ```

    Add a comment block at the top of render.yaml:
    ```yaml
    # MemoraLabs Render Deployment
    # Persistent disk at /data -- NOT available during build
    # Keep-alive: Configure UptimeRobot (free) to GET /health every 10 minutes
    # UptimeRobot URL: https://memoralabs.onrender.com/health
    ```
  </action>
  <verify>
    Run `cd /Users/sevs/memoralabs && python -c "from app.main import app; print(app.title, app.version)"` -- should print "MemoraLabs 0.1.0".
  </verify>
  <done>FastAPI app with lifespan wiring system DB + TenantDBManager, health router mounted, render.yaml with persistent disk config.</done>
</task>

<task type="auto">
  <name>Task 2: Health endpoint integration test + full test suite run</name>
  <files>
    tests/test_health.py
  </files>
  <action>
    Create `tests/test_health.py` using httpx.AsyncClient + pytest-asyncio:

    - Import `AsyncClient` from httpx, `ASGITransport` from httpx, `app` from `app.main`
    - Use `ASGITransport(app=app)` with `AsyncClient` for in-process testing (no server needed)

    - Async fixture `client`:
      ```python
      async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as client:
          yield client
      ```
      Note: The lifespan runs automatically when AsyncClient is used with ASGITransport. Set env var DATA_DIR to a tmp_path before creating client. Use `monkeypatch` or `os.environ` to set DATA_DIR to `str(tmp_path)` before importing/using the app. The cleanest approach: use a fixture that patches `app.config.DATA_DIR` to tmp_path before the client starts.

      Actually, the simplest reliable approach: use `@pytest.fixture` that sets `os.environ["DATA_DIR"] = str(tmp_path)` and then reimports/patches config. OR: override `app.state` manually. Prefer the env var approach:
      ```python
      @pytest.fixture
      def anyio_backend():
          return "asyncio"

      @pytest_asyncio.fixture
      async def client(tmp_path, monkeypatch):
          monkeypatch.setenv("DATA_DIR", str(tmp_path))
          # Patch the already-imported config module
          import app.config as config
          monkeypatch.setattr(config, "DATA_DIR", tmp_path)
          async with AsyncClient(
              transport=ASGITransport(app=app),
              base_url="http://test"
          ) as client:
              yield client
      ```

    - `test_health_returns_200`: GET /health, assert status 200
    - `test_health_response_fields`: GET /health, parse JSON, assert "status" == "healthy", "timestamp" is a string, "version" == "0.1.0"
    - `test_health_timestamp_is_utc_iso`: GET /health, parse timestamp with `datetime.fromisoformat()`, assert no error
    - `test_root_returns_404_or_redirect`: GET /, assert status is not 500 (either 404 or a future landing page -- just verify the app doesn't crash)

    After writing, run the FULL test suite:
    `python -m pytest tests/ -v` -- ALL tests across all 4 test files must pass.

    If any prior tests broke due to import issues or missing fixtures, fix them.
  </action>
  <verify>
    Run `cd /Users/sevs/memoralabs && python -m pytest tests/ -v` -- ALL tests across test_system_db.py, test_tenant_db.py, test_manager.py, test_isolation.py, test_health.py pass.
    Run `cd /Users/sevs/memoralabs && uvicorn app.main:app --port 8111 &` then `curl http://localhost:8111/health` returns 200 with healthy status. Kill the server after.
  </verify>
  <done>Health endpoint returns 200 with correct JSON. Full test suite (all 5 test files, 25+ tests total) passes. Server starts and responds to /health.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -v` -- ALL tests pass (system DB, tenant DB, manager, isolation, health)
- `uvicorn app.main:app` starts without errors
- `curl localhost:8000/health` returns `{"status": "healthy", "timestamp": "...", "version": "0.1.0"}`
- render.yaml exists with persistent disk at /data
- System DB created at {DATA_DIR}/system.db on startup
</verification>

<success_criteria>
- /health returns 200 with status, timestamp, version (INFRA-06)
- Lifespan initializes system DB + TenantDBManager (INFRA-01, INFRA-03)
- Lifespan cleans up on shutdown (INFRA-03)
- render.yaml configures persistent disk at /data (INFRA-04)
- Keep-alive documented via UptimeRobot (INFRA-07)
- Full test suite passes end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
