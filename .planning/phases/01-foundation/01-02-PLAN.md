---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/db/tenant.py
  - migrations/002_tenant_tables.sql
  - tests/test_tenant_db.py
autonomous: true

must_haves:
  truths:
    - "Tenant DB creates memories, entities, relations, feedback tables with all ZimMemory v15 columns"
    - "memories table includes embedding BLOB DEFAULT NULL column for Phase 2"
    - "Foreign keys enforced: relations reference entities and memories, feedback references memories"
    - "All performance indexes exist on tenant tables"
  artifacts:
    - path: "app/db/tenant.py"
      provides: "Tenant DB schema init function"
      exports: ["TENANT_SCHEMA_SQL", "init_tenant_db"]
    - path: "migrations/002_tenant_tables.sql"
      provides: "Raw SQL for tenant tables"
      contains: "CREATE TABLE IF NOT EXISTS memories"
    - path: "tests/test_tenant_db.py"
      provides: "Tenant DB schema tests"
      min_lines: 40
  key_links:
    - from: "app/db/tenant.py"
      to: "aiosqlite"
      via: "async connection for schema init"
      pattern: "aiosqlite.connect"
---

<objective>
Create the per-tenant database schema module: memories, entities, relations, and feedback tables ported from ZimMemory v15, with an added embedding BLOB column for Phase 2 readiness.

Purpose: Every tenant gets their own SQLite database with this schema. This module defines WHAT goes into each tenant DB; the TenantDBManager (Plan 03) handles WHERE and HOW connections are managed.
Output: Tenant schema SQL, init function, and passing tests.
</objective>

<execution_context>
@/Users/sevs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sevs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tenant DB schema SQL + init function</name>
  <files>
    migrations/002_tenant_tables.sql
    app/db/tenant.py
  </files>
  <action>
    Create `migrations/002_tenant_tables.sql` with the exact ZimMemory v15 schema from RESEARCH.md, plus one addition:

    **memories table** -- all 19 columns from ZimMemory PLUS `embedding BLOB DEFAULT NULL` (added after `shared_with`, before closing paren). This avoids a Phase 2 migration. Total: 20 columns.

    **entities table** -- all 11 columns exactly as in RESEARCH.md.

    **relations table** -- all 15 columns with 3 foreign keys (source_entity_id, target_entity_id, memory_id) exactly as in RESEARCH.md.

    **feedback table** -- all 5 columns with FK to memories(id) exactly as in RESEARCH.md.

    **schema_version table** -- same pattern as system DB: version INTEGER PK, applied_at INTEGER, description TEXT.

    **All indexes from RESEARCH.md:**
    - idx_memories_user, idx_memories_agent, idx_memories_session, idx_memories_hash, idx_memories_created, idx_memories_deleted
    - idx_entities_name (on name_normalized), idx_entities_type
    - idx_relations_source, idx_relations_target, idx_relations_memory

    INSERT OR IGNORE schema_version (1, current_timestamp, '002_tenant_tables').

    Create `app/db/tenant.py`:
    - `TENANT_SCHEMA_SQL`: string constant with the full SQL (inlined, not read from file)
    - `async def init_tenant_db(conn: aiosqlite.Connection) -> None`: executescript TENANT_SCHEMA_SQL, commit. Does NOT open the connection -- receives an already-open connection (TenantDBManager opens connections). This separation is intentional: manager handles connections, this module handles schema.
  </action>
  <verify>
    Run `cd /Users/sevs/memoralabs && python -c "from app.db.tenant import TENANT_SCHEMA_SQL, init_tenant_db; print('OK'); print(f'{len(TENANT_SCHEMA_SQL)} chars')"` -- should print OK and a char count > 1500.
  </verify>
  <done>TENANT_SCHEMA_SQL contains all 4 tables + schema_version + 11 indexes + embedding column. init_tenant_db applies schema to a provided connection.</done>
</task>

<task type="auto">
  <name>Task 2: Tenant DB schema tests</name>
  <files>
    tests/test_tenant_db.py
  </files>
  <action>
    Create `tests/test_tenant_db.py` using pytest-asyncio:

    - Async fixture `tenant_conn`: opens an aiosqlite connection to a temp file (use `tmp_path`), sets WAL + foreign_keys ON, calls `init_tenant_db(conn)`, yields conn, closes on teardown.

    - Test `test_tables_created`: query `sqlite_master` for table names, assert memories, entities, relations, feedback, schema_version all exist.

    - Test `test_memories_columns`: PRAGMA table_info(memories), assert all 20 columns exist including `embedding`. Verify `text` is NOT NULL, `id` is PRIMARY KEY, `embedding` default is NULL.

    - Test `test_entities_columns`: PRAGMA table_info(entities), assert all 11 columns. Verify `name` NOT NULL, `entity_type` NOT NULL.

    - Test `test_relations_foreign_keys`: PRAGMA foreign_key_list(relations), assert 3 FKs exist (source_entity_id -> entities, target_entity_id -> entities, memory_id -> memories).

    - Test `test_feedback_foreign_key`: PRAGMA foreign_key_list(feedback), assert FK to memories(id).

    - Test `test_indexes_exist`: PRAGMA index_list for memories, entities, relations. Assert all 11 named indexes exist.

    - Test `test_schema_version_populated`: SELECT from schema_version, assert version=1 exists.

    - Test `test_insert_memory_with_embedding_null`: INSERT a memory row with embedding=NULL, verify it succeeds and embedding reads back as None. This confirms the column exists and accepts NULL.

    - Test `test_foreign_key_enforcement`: Try to INSERT a relation with a non-existent source_entity_id. Assert it raises IntegrityError (proves foreign_keys=ON works).
  </action>
  <verify>
    Run `cd /Users/sevs/memoralabs && python -m pytest tests/test_tenant_db.py -v` -- all tests pass.
  </verify>
  <done>9+ tests pass covering table existence, column correctness, foreign key enforcement, index existence, schema version, and embedding column readiness.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_tenant_db.py -v` -- all tests pass
- TENANT_SCHEMA_SQL includes `embedding BLOB DEFAULT NULL` in memories table
- Foreign key enforcement verified by test (IntegrityError on invalid FK)
</verification>

<success_criteria>
- app/db/tenant.py exports TENANT_SCHEMA_SQL and init_tenant_db
- Schema matches ZimMemory v15 exactly (memories 20 cols, entities 11, relations 15, feedback 5)
- embedding column present in memories for Phase 2
- All 11 indexes created
- Foreign keys enforced and tested
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
